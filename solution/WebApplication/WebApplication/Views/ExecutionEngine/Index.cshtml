@using Humanizer; 
@model IEnumerable<WebApplication.Models.ExecutionEngine>

@{
    ViewData["Title"] = "Index - " + "WebApplication.Models.ExecutionEngine".Pluralize().Humanize(LetterCasing.Title);
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1> @("WebApplication.Models.ExecutionEngine".Pluralize().Humanize(LetterCasing.Title))</h1>

<p>
    <a class="btn-sm btn-dark details" asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead class="thead-dark">
        <tr>
            <th></th>
             <th>
                @Html.DisplayNameFor(model => model.EngineId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EngineName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResourceGroup)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubscriptionUid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DefaultKeyVaultURL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LogAnalyticsWorkspaceId)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EngineId" class="btn-sm btn-warning details" title="Edit"><i class="fa fa-pencil-square-o"></i></a> |
                <a asp-action="Details" asp-route-id="@item.EngineId" class="btn-sm btn-info details" title="Details"><i class="fa fa-list-alt" aria-hidden="true"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.EngineId" class="btn-sm btn-danger details" title="Delete"><i class="fa fa-trash"></i></a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EngineId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EngineName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResourceGroup)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubscriptionUid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DefaultKeyVaultURL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LogAnalyticsWorkspaceId)
            </td>
        </tr>
}
    </tbody>
</table>
